pipeline {
    agent {
        label 'docker-agent'
    }

    stages {
        stage('Hello') {
            steps {
                echo 'Hello from GitHub + Jenkins!'
            }
        }
    }
}
